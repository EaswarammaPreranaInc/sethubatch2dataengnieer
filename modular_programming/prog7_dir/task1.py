#  Find  outputs  (Home  work)
import  sys , time , math
print(dir(sys))   #all the memebers of the module, its imports and the environment variables
print()
print()
print(dir(time))  #all the memebers of the module, its imports and the environment variables
print()
print(dir(math))  #all the memebers of the module, its imports and the environment variables