#  What  are  the  outputs  if  inputs  are  25 , Rama  Rao ,  male , 52 , 48 , 55   (Home  work)
from  prog9a  import  student
s = student()
print(s . dict) # {}
s . get()
print(s . dict) # {'rno':45,'sname':'giri','gender':'male','m':[52,48,46]}
s . compute()
print(s . dict) #{'rno': 25, 'sname': 'Rama Rao', 'gender': 'male', 'm': [52, 48, 55], 'tot': 155, 'avg': 51.67, 'grade': 'Second class'}
'''
Repeat  student  program  for  'n'  students

1) import  student  class  defined in  prog9a  but  do  not  rewrite

2) Use  list  of  objects
'''
from prog9a import Student
students=[]
n=int(input("number of students:"))
for i in range(n):
    s = student()     # create object
    s.get()           # input details
    s.compute()       # compute total, avg, grade
    students.append(s)  # add to list
for i in students:
    i.disp()
    print(i)
#  dir()  function  demo  program  (Home  work)

from  prog10a   import  Rat

a = Rat()

a . nr = 22

a . dr = 7

print(dir(Rat)) # ['_class', 'delattr', 'dict', 'dir', 'doc','str', 'init','get','test','str_','add','sub','mul','div','simplify']

print()

print()

print(dir(a)) # ['_class', 'delattr', 'dict', 'dir', 'doc','str', 'init','get','test','str_','add','sub','mul','div','simplify','nr','dr']
    
#  Find  outputs  (Home  work)
class      Rat:
	def    m1():
		pass
# End  of  the  class
a = Rat()
a . nr = 22
print(hasattr(a , 'nr')) # True
print(hasattr(a , 'dr')) # False
print(hasattr(a , 'm1')) # True
print(hasattr(a , 'm2')) # False
print(hasattr(Rat , 'm1')) # True
print(hasattr(Rat , 'm2')) #False
print(hasattr(Rat , 'nr')) # False
# Find  outputs  (Home  work)
class  Cat:
	def  talk(self):
		print('Meow Meow Meow ....')
class  Dog:
	def  bark(self):
		print('Bhow Bhow Bhow ....')
class  Goat:
	def  talk(self):
		print('Mehar  Mehar  Mehar  ....')
#end of the class
a = [Cat() , Dog() , Goat()]
for  x  in   a:
	if   hasattr(x , 'talk'):
		x . talk()
	else:
		x . bark()
'''
Meow Meow Meow ....
Bhow Bhow Bhow ....
Mehar  Mehar  Mehar  ....  '''
#  Find  outputs  (Home  work)
class    c1:
        pass
a = c1()
a . x = 10
varname = input('Enter  variable  name  to  be  added  to  object  :  ')   #  Assume  that  input  is  'y'
value = eval(input('Enter  value  of  the  variable  :  '))   #  Assume  that  input  is   20
setattr(a , varname , value) 
print(a . dict) #{'y':20}
print(a . x) # 10
while  True:
	try:
		varname = input('Enter  variable  name  whose  value  is  to  be  retrieved  :  ')
									#  Assume  that  input  is  x  in  1st   iteration  ,  y  in   2nd   iteration  and  z  in  3rd  iteration
		print(getattr(a , varname))   # 10 \n 20 /n z has error
	except:
		print(F'Invalid  variable   name   :  {varname}')  # Invalid  variable   name   : z
		break
'''
(Home  work)
Write  a  program  to  convert  a  dictionary  {'Empno' : 25 , 'Ename' : 'Rama  Rao' , 'Sal' : 10000.0}  to  Emp  class  object
i.e.  object  should  contain  empno = 25 , ename = 'Rama  Rao' , Sal = 10000.0

Hint:  Use  setattr()  and  getattr()  functions
'''
class  Emp:
        pass
#End  of  the  class
dict = {'Empno' : 25 , 'Ename' : 'Rama  Rao' , 'Sal' : 10000.0}
e=Emp()
# How  to  convert  dictionary  to  object  'e'  with  for  loop
for k,v in dict.items():
    setattr(e,k,v)
# How  to  print  object  'e'  with  for  loop
for k in data.keys():
    print(f"{k} : {getattr(e, k)}")

# Repeat prog10a with 3 objects
# Import the rat class from prog10a (assuming it's in the same directory or already defined)
'''
Repeat  prog10a  with  3  objects

Eg:  c = a + b
	 print  c
	 c = a - b
	 print  c
	 c = a * b
	 print  c
	 c = a / b
	 print  c

Hint:  Import   Rat  class  defined  in  prog10a  but  do  not  define  Rat  class   again
'''
from prog10a import rat  # Do not redefine class rat

# Create 3 objects
a = rat()
b = rat()
c = rat()

# Input values for a and b
print("Enter first rational number:")
a.get()

print("Enter second rational number:")
b.get()

# Perform addition
c.add(a, b)
print("a + b =", c)
'''
Repeat  prog10a  with  list  of  6  objects

Hint:  import  Rat  class  defined  in  prog10a  but  do  not  rewrite  the  class  again

What  are  the  object  names  ?  --->  a[0] , a[1] , a[2] , .....a[5]
'''
# Repeat prog10a with a list of 6 objects
# Import the rat class from prog10a (assuming it's in the same directory or already defined)

from prog10a import rat  # Do not redefine class rat

# Create a list of 6 objects
a = [rat() for _ in range(6)]

# Input values for each object
for i in range(6):
    print(f"Enter rational number for a[{i}]")
    a[i].get()

# Perform operations with the objects in the list

# Example: add all pairs
for i in range(5):  # Loop over the first 5 elements for addition with the next one
    c = rat()
    c.add(a[i], a[i+1])
    print(f"a[{i}] + a[{i+1}] =", c)

# Example: subtract all pairs
for i in range(5):
    c = rat()
    c.sub(a[i], a[i+1])
    print(f"a[{i}] - a[{i+1}] =", c)

# Example: multiply all pairs
for i in range(5):
    c = rat()
    c.mul(a[i], a[i+1])
    print(f"a[{i}] * a[{i+1}] =", c)

# Example: divide all pairs
for i in range(5):
    c = rat()
    c.div(a[i], a[i+1])
    print(f"a[{i}] / a[{i+1}] =", c)


# Perform subtraction
c.sub(a, b)
print("a - b =", c)

# Perform multiplication
c.mul(a, b)
print("a * b =", c)

# Perform division
c.div(a, b)
print("a / b =", c)